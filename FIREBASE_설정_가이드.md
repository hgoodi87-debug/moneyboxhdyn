# MoneyBox · Firebase 연결 설정 가이드 (비개발자용)

> 목표: 여러 기기(매장 PC·직원 폰)에서 **같은 데이터를 실시간 공유**.
> 한 번만 설정하면 됩니다. **설정값은 채팅·깃허브에 안 올라가고, 사장님 브라우저에만 저장**됩니다.

---

## 1단계 · 프로젝트 만들기
1. [console.firebase.google.com](https://console.firebase.google.com) 접속 → 구글 로그인
2. **프로젝트 추가** → 이름 `moneybox` 입력 → 계속
3. Google 애널리틱스는 **사용 안 함**으로 꺼도 됩니다 → **프로젝트 만들기**

## 2단계 · 데이터베이스 만들기 (서울 서버)
1. 왼쪽 메뉴 **빌드 → Firestore Database** → **데이터베이스 만들기**
2. 위치: **`asia-northeast3 (Seoul)`** 선택 ⚠️ (한 번 정하면 못 바꿉니다)
3. **프로덕션 모드로 시작** 선택 → **만들기** (처음부터 잠금 = 안전)

## 3단계 · 보안 규칙 붙여넣기
1. Firestore Database 화면 위쪽 **[규칙]** 탭 클릭
2. 안에 있던 내용을 **전부 지우고**, 프로젝트 폴더의 **`firestore.rules`** 파일 내용을 **통째로 붙여넣기**
3. **게시** 버튼 클릭
   → 이제 "로그인한 사람만" 데이터에 접근할 수 있습니다.

## 4단계 · 익명 로그인 켜기
1. 왼쪽 메뉴 **빌드 → Authentication** → **시작하기**
2. **Sign-in method** 탭 → 목록에서 **익명(Anonymous)** 클릭 → **사용 설정** 켜기 → **저장**

## 5단계 · 웹앱 등록하고 설정값 복사
1. 왼쪽 위 **⚙️ → 프로젝트 설정**
2. 아래로 스크롤 → **내 앱** → 웹 아이콘 **`</>`** 클릭
3. 앱 닉네임 `moneybox-web` 입력 → **앱 등록**
4. 화면에 나오는 **`const firebaseConfig = { … }`** 코드를 **통째로 복사**

## 6단계 · 우리 앱에 연결 (여기서 끝!)
1. 브라우저에서 **`setup.html`** 페이지 열기 (예: `http://localhost:8899/setup.html`)
2. 복사한 **firebaseConfig 전체를 붙여넣기**
3. **🔍 연결 테스트** → "✅ 연결 성공" 확인
4. **💾 저장하고 시작** 클릭 → 끝!

> 🔒 이 설정은 **이 브라우저에만** 저장됩니다. 깃허브·서버·AI 어디에도 올라가지 않습니다.
> 화면이 잘 안 바뀌면 **Ctrl + Shift + R** (강력 새로고침) 한 번 해주세요.

---

## 다른 기기에서도 쓰려면 (나중에)
지금은 사장님 PC(localhost)에서만 열립니다. 직원 폰·다른 매장 PC에서도 쓰려면
앱을 인터넷에 올려야 하는데(**Firebase Hosting**, 무료·HTTPS·한국 빠름), **제가 도와드릴게요.**
올린 뒤 각 기기에서 **setup.html 한 번씩** 같은 설정을 넣으면 모두 실시간 연동됩니다.

## 자주 묻는 것
- **"apiKey가 노출되면 위험하지 않나요?"** — 이 값은 비밀번호가 아니라 "주소표" 같은 거예요. 진짜 보안은 **3단계 규칙 + 4단계 로그인**이 막아줍니다. 그래도 안심되게, 우리는 이 값을 깃허브에 안 올리고 브라우저에만 둡니다.
- **"데이터를 AI가 보나요?"** — 아니요. 사장님 전용 Firestore에만 저장되고, 구글이 AI 학습에 쓰지 않으며, 저(클로드)도 접근할 수 없습니다.
- **"30일 뒤 잠긴다던데?"** — 그건 *테스트 모드*일 때 얘기예요. 우리는 **프로덕션 모드 + 규칙**으로 시작하니 만료 없이 계속 안전합니다.
